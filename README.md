# Words with Spouses

A private, asynchronous two-player word-tile game built in Rust with HyperChad.

The repository is under active implementation. See `INVARIANTS.md` for durable architectural boundaries and `AGENTS.md` for contributor validation requirements.
